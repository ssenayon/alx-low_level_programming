#include <stdio.h>
/**
 * main - backward alphabets
 *
 * Return:  Always 0 (success)
 */
int main(void)
{
	char n;
	for (char = 'z'; char >= 'a'; char--)
		putchar ("%d", n);
	putchar ('\n');
	return (0);
}
