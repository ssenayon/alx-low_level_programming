#include <stdio.h>

/**
 * main - print alphabet
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char did;

	for (did = 'a'; did <= 'z'; did++)
	putchar (did);
	putchar ('\n');
	return (0);
}
